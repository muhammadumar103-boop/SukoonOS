# Features

Each product area should get its own folder here once approved.

Suggested shape:

```text
src/features/example/
  actions/
  components/
  data/
  schemas/
  types.ts
```

Do not add feature folders until the feature is accepted for the roadmap.
